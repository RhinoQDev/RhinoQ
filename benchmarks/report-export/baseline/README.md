# Baseline implementation slot

Place the independent application implementation here. It must pass the same
commands declared in `../acceptance.json`. This placeholder is deliberately
not counted as source code.
