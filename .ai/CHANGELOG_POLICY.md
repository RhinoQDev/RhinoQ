# Changelog policy

Ghi vào `CHANGELOG.md` khi thay đổi public API, behavior, schema, CLI, config, operational behavior hoặc compatibility.

Không ghi implementation detail nội bộ nếu không ảnh hưởng người dùng. Mỗi entry phải nói rõ migration action nếu cần.
