{
  "name": "rhinoq-app",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node start.mjs",
    "server": "node --env-file=.env server.mjs",
    "verify": "node --env-file=.env verify.mjs",
    "doctor": "node --env-file=.env node_modules/.bin/rhinoq doctor",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down -v"
  },
  "dependencies": {
    "@rhinoq/node": "0.1.0-beta.10",
    "bullmq": "^5.81.3",
    "express": "^4.22.2",
    "ioredis": "^5.11.1",
    "pg": "^8.22.0"
  },
  "engines": {
    "node": ">=22"
  }
}
